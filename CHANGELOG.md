# Changelog

## [Unreleased]

### Added

- メニューバーを2行表示（上段に枠の名前・下段に使用率）にできるようにした。設定の全般タブで1行 / 2行を選べる。表示モードとは独立した設定で、枠の選び方は1行と同じ。
- 枠種別（5時間枠 / 週間枠 / モデル固有枠）の表示順を Codex / Claude それぞれの設定タブから変更できるようにした。ポップオーバーとメニューバーの両方へ反映する。メニューバーに表示する枠の選び方（使用率の高い枠を優先）は変更していない。
- メニューバーのプロバイダー接頭ラベル（CX / CL）を設定画面から provider ごとに任意文字へ変更できるようにした（空欄で既定へ復帰・6文字上限）。
- 取得済みの使用量をコマンドラインから読めるようにした（`swift run takometa usage [--json]`）。エージェントやシェルから残量を確認できる。値はアプリが取得済みのキャッシュで、コマンド自体は通信しない（JSON では取得時刻と経過秒数、通常表示では読みやすい相対経過時間を出力する）。
- OpenAI / Anthropic のサービス障害をポップオーバーに表示するようにした。「上限に達したのか障害なのか」を切り分けられる。障害情報を取得できないときは正常と区別して表示する（取得できないことを「障害なし」と表示しない）。通常時は行が増えない。
- 直近の消費ペースを平均ペースと併記するようにした（「平均 3.2%/h・直近 8.1%/h」）。今のペースが平均より速いか遅いかが分かる。算出のため使用量の履歴をローカルに保持する（窓ごとに直近48時間・最大1000点）。生のモデル名や識別子は保存せず、ハッシュ化した短い値で窓を区別する。上限到達予測は従来どおり平均から計算する。

### Changed

- 通知の閾値を変更したとき、同じ枠が続いていても新しい閾値で判定し直すようにした。従来は次の枠に切り替わるまで再通知されなかった（週間枠では最長7日）。
- 通知とポップオーバーの日時表示で、当日・翌日・前日を「本日 / 明日 / 昨日」と表示するようにした（例:「このペースだと本日 16:20 に上限へ到達します」）。それ以外の日付表示は従来どおり。

## [0.1.0] - 2026-07-22

- 配布物を DMG 形式にし、アプリを Applications フォルダへドラッグしてインストールできるようにした。

- Codex と Claude Code の使用済みレート制限を macOS メニューバーに表示する MVP を追加。
- 5時間・週間・モデル固有枠、更新時刻、平均ペース、上限到達予測をポップオーバーに表示。
- Full / Balanced / Compact の表示モードとログイン時起動設定を追加。
- 最終成功値のキャッシュ、認証状態、通信失敗時のバックオフと鮮度表示を追加。
- 使用率閾値、上限到達予測、上限到達、上限からの回復、1日の消費量の5種類の通知を追加。
- 通知の有効化、使用率閾値、日次通知、日次閾値を設定画面から変更できる機能を追加。
- Codex と Claude Code のメニューバー項目を1つに統合。
- メニューバーに表示するプロバイダーと枠種別を設定画面から選べる機能を追加。
- 設定画面を「全般」「Codex」「Claude」の3タブ構成に変更。
- 通知の有効化と閾値を Codex・Claude ごとに設定できる機能を追加（旧通知設定は引き継がれないため再設定が必要）。
- プロバイダーごとの表示・通知設定を JSON に統合し、未知のプロバイダー設定を保持できる構造へ変更。
- 設定画面からプロバイダーの表示順を並べ替え、メニューバー表示へ反映できる機能を追加。
- ポップオーバーにアプリの終了、バージョン、ビルドコミットの表示を追加。
- universal binary の `.app`、受け取り手向け案内、SHA256 を含む身内向け zip 配布手順と検証ゲートを追加。
