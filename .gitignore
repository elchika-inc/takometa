# macOS
.DS_Store

# Xcode
build/
DerivedData/
*.xcuserstate
xcuserdata/
*.moved-aside
*.hmap
*.ipa

# Swift Package Manager
.build/
.swiftpm/
Package.resolved
dist/

# 秘密・実トークンを含む可能性のあるファイル（fixture は秘密除去済みのみコミット可）
*.secret.*
.env
.env.*

# エージェントが生成するローカル専用の作業物。マシン固有の絶対パス・エージェント名・
# 一時状態を含み、他の環境では意味を持たないためリポジトリに含めない。
# ローカルには配置したままでよい（ignore は追跡対象から外すだけで、削除はしない）。
.codex/
.superpowers/
docs/superpowers/
.worktrees/
.claude/worktrees/
.playwright-mcp/
